{
  "name": "dokku-dashboard",
  "description": "Simple Dokku projects dashboard",
  "keywords": ["dokku", "dashboard"],
  "env": {
    "DASHBOARD_PASSWORD": {
      "description": "Password for dashboard access",
      "required": true
    },
    "SESSION_SECRET": {
      "description": "Long random secret used to sign dashboard sessions (required in production)",
      "required": true
    },
    "METRICS_DB_PATH": {
      "description": "SQLite metrics database on persistent storage",
      "required": true
    }
  }
}
